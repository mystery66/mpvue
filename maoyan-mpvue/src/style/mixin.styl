two()
  text-overflow ellipsis
  white-space nowrap
  overflow hidden

wh($width, $height) 
   width $width
   height $height

sc($fz, $c)
  font-size $fz
  color $c   

p($position, $top, $left, $right, $bottom)
  position $position
  top $top
  left $left
  right $right
  bottom $bottom